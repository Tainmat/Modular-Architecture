import React from "react";
import ReactDOM from "react-dom/client";
import { SemModularizacao } from "./pages/SemModularizacao/SemModularizacao.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SemModularizacao />
  </React.StrictMode>
);
